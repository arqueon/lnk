// ═══ user.kdl (POR-HOST: casa) ═══
// Solo lo específico de casa; lo compartido vive en user-common.kdl.
// Último include de config.kdl: gana a todo.

window-rule {
    match app-id="google-chrome"
    default-column-width { proportion 0.5; }
    open-focused true
}

window-rule {
    match app-id="firefox"
    default-column-width { proportion 0.5; }
    open-focused true
}

/- window-rule {
    match app-id=r#"^(firefox|org\.mozilla\.firefox)(-.*)?$"#
    match app-id=r#"^google-chrome(-.*)?$"#
    match app-id=r#"^com\.google\.Chrome(-.*)?$"#
    open-on-workspace "web"
}


// ── Arranque de triple instancia de Logseq (casa, ofi, laptop) ──
spawn-at-startup "logseq-multi-launch"

// ── Binds propios ──
binds {
    Mod+F4 { spawn "logseq-multi-launch"; }
    Mod+F7 { spawn "env" "GDK_BACKEND=x11" "virt-manager" "--connect" "qemu:///system" "--show-domain-console" "win11"; }
}
